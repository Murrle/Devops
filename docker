pipeline {
    agent { docker { image 'openjdk:latest' } }
      stages {
        stage('log version info') {
      steps {
        sh 'docker run -d --name mm12 openjdk'
        docker stop mm12
      }
    }
  }
}
