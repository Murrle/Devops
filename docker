pipeline {
    agent { docker { image 'openjdk:latest' } }
      stages {
        stage('log version info') {
      steps {
        sh 'docker run -d --name test1 openjdk'
        sh 'docker stop test1'
      }
    }
  }
}
