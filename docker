pipeline {
    agent { docker { image 'openjdk:latest' } }
      stages {
        stage('log version info') {
      steps {
        sh 'docker run -d --name Murali openjdk:latest'
      }
    }
  }
}
